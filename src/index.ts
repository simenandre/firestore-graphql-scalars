import * as resolvers from './resolvers';

export { Timestamp as TimestampTypeDefinition } from './typeDefs';

export { default as typeDefs } from './typeDefs';

export { Timestamp as TimestampResolver } from './resolvers';

export { resolvers };
